#include <iostream>
using namespace std;
int main() {
  int n, m;
  cin >> n >> m;
  getchar();
  string atr = to_string(a + b);
  int len = str.size();
  for (int i = 0; i < len; ++i) {
    cout << str[i];
    if (s[i] == '-') continue;
    if ((i + 1) % 3 == len % 3 && i != len - 1) cout << ",";
  }
  return 0;
}
